{
  "name": "cockpit-mcp",
  "version": "0.4.0",
  "description": "Cockpit MCP server — exposes workspaces/cards/specs as tools for Claude Code",
  "type": "module",
  "private": true,
  "bin": {
    "cockpit-mcp": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile=dist/cockpit-mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^24.12.2"
  }
}
